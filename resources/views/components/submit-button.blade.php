
    <button class="left-0 flex p-2 mx-2 mt-4 mb-2 font-sans text-base text-gray-600 rounded-md outline outline-1 outline-gray-300 hover:shadow-lg hover:bg-slate-200" type="{{ $type }}" onclick="{{ $onclick }}" style="display: {{ $style }}" name="{{ $name }}" id="{{ $id }}">{{ $slot }}</button>

